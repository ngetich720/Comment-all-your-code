# This is a simple Python script that calculates the factorial of a given number.
# It demonstrates proper commenting practices.

# Define a function to calculate factorial
def factorial(n):
    """
    This function takes an integer 'n' as input and returns its factorial.
    
    Args:
    n (int): The input integer
    
    Returns:
    int: The factorial of 'n'
    """
    
    # Initialize the result to 1
    result = 1
    
    # Iterate from 1 to n (inclusive) and multiply each number to the result
    for i in range(1, n + 1):
        result *= i
    
    # Return the final result
    return result

# Prompt the user for input
user_input = int(input("Enter an integer: "))

# Call the factorial function and print the result
print(f"The factorial of {user_input} is {factorial(user_input)}")
